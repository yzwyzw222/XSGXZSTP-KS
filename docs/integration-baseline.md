# 集成基线与已验证项目状态

核对日期：2026-09-11。本文件与 `development.md` 是仓库指定的集成项目记忆。当前系统范围以 `deploy/systems.json` 为准，旧验收文档仅记录其对应阶段。

## 当前系统与来源

按用户要求，当前仅保留学术关系知识图谱构建平台与学术成果信息采集及可视化系统。extraction、scholar 已从有效源码、门户入口、构建、启停、初始化和样例导入范围中删除。门户卡片编号为 01、02，日志筛选仅提供门户及两个有效系统。

| 系统 | 显示名称 | 核定来源 | 来源 SHA |
| --- | --- | --- | --- |
| relation | 学术关系知识图谱构建平台 | 已导入的 feature/Ye 完整版 | `184f651b223efe9a07ea6d352146e5df34a537da` |
| crawler | 学术成果信息采集及可视化系统 | feature/Luo | `3a0127019052c182962d6630981e0acec2d66d53` |

仓库为 `https://github.com/yzwyzw222/XSGXZSTP-KS.git`，当前在 `dev` 分支维护。历史导入记录保留在 `import-records.json`；两个删除系统明确标记 retired。2026-09-11 本机检查确认已退役 scholar 的来源对象缺失，完整历史来源检查受阻，详见 [研究页面验证记录](compact-research-ui.md#项目记忆与范围)。`source-adaptations.json` 仅维护有效系统的适配哈希；来源检查已通过两个有效系统的全部原始文件、新增文件和适配差异。系统删除实施阶段未创建提交或推送；后续交付沿用 `dev` → `origin/dev`，按采集能力、门户及系统整合、图谱修复分批提交，各批包含相应项目记忆和来源适配记录，不改写已有历史。

## 2026-09-11 三类学术图谱

crawler 知识图谱拆为学术关系、学术成果、学术背景三个作者视图。实体展示为作者、专利、论文、指导硕论和指导博论；存储及投影类型仍兼容现有 `Author`、`Achievement`、`AUTHORED` 和 `SUPERVISED`。新增作者分页查询，以真实共同署名派生中心作者的合作边；背景时间线按日期升序、空日期末尾排序，分页不再因中心作者超过 300 项成果而遗漏后续页面。

三个独立主导航入口为 `/academic-relations`、`/academic-achievements`、`/academic-background`，`/graph` 及原 `/graph/relations` 等地址兼容跳转并保留作者与查询参数；作者导入与工作台链接转到成果图谱。原全局概览在 `/graph/overview`，关系样式使用 `/graph/settings/edges`，旧 `/graph/relations` 别名改为新业务模块。三模块的主导航、侧栏与命令面板保留当前作者，旧作品中心链接仍可进入高级查询。

本次已核对并同步本文件、`development.md` 和作者导入说明。隔离测试完成后，按用户试用要求于 2026-09-11 构建 crawler 前后端并替换其运行 JAR，整体、readiness 和 graph 均为 `UP`；V17 已是最新版本，无新迁移。门户和 relation 原进程保持运行，未变更依赖或执行 Git 提交。三个业务入口按既有认证规则导向统一登录；已打开试用入口，登录后的真实图谱交互待用户试用。实际构建命令、验证与备份位置见 [三类学术图谱](academic-graphs.md)，下方旧运行验收不替代本次启用记录。

## 2026-09-11 独立图谱与研究页面空间调整

图谱工具栏合并、内容清单默认收起，主画布占据剩余空间。实体编目新增专利、指导硕论、指导博论，和作者、机构、期刊一起展示内部标识与中文类型。新增三类目录查询复用规范成果及人工修订字段，导师关系以 MySQL 中真实的 achievement_advisor 为依据，不将本人署名硕博论文当作指导论文。

成果目录删除大筛选区，改为右上角选择字段的单框搜索，日期列日历筛选年份，导出操作收进小按钮；不恢复旧 URL 中的类型和来源条件。统计页同样压缩搜索、导航、指标和导出区域，保留既有机构/主题规范 ID 查询和年份范围语义。统计 CSV/JSON 导出已加载的聚合结果，类型与来源分布图保留。接口鉴权、目录导出票据和下载校验保持原状，无依赖、数据库结构或历史数据变化。

本轮读取并同步本文件、development.md、academic-graphs.md；原父菜单和子路由描述与新需求不再一致，已通过路由、导航、SQL 和测试核对并修正。当前准确的验证命令、文件清单及本机运行状态见 [研究页面调整](compact-research-ui.md)，前述首轮试用记录不替代本轮启用验证。

## 2026-09-11 作者信息表导入

crawler 的业务入口已调整为学者资料解析与图谱展示：新增 `/author-import`，移除数据源、采集任务、数据治理和质量指标四个页面及其控制器。首页和大屏显示导入记录。现有规范成果、来源证据、治理结果、权限体系、URL 前缀与 Outbox 架构继续使用；保留旧数据表和内部读取依赖，不执行历史业务数据删除。旧 Quartz 采集 Job 在触发时注销自身，启动恢复与配额恢复调度不再装配。

V17 新增作者导入身份、批次、成果标识、原始字段和导师/机构关系表；主题的远程 source_id 改为可空以保存文件关键词。POI 5.5.1 与 Commons CSV 1.14.1 分别解析工作簿和 CSV。新增 `AUTHOR_IMPORT` 权限，ADMIN 与 DATA_OPERATOR 可操作；RESEARCHER 可以查询成果和原始导入证据。

解析使用用户确认的 18 个知网列名，保存全部原始值。本人署名按 `AUTHORED` 投影，明确选择的硕博导师按 `SUPERVISED` 投影；成果单位按 `PRODUCED_AT` 连接机构。只有用户确认的学者所属机构才生成作者所属关系。摘要作为成果属性保留。导入预览与确认绑定文件内容和设置；错误行阻止整批导入，重复请求返回已完成批次。

本轮读过本文件和 `development.md`，并对照现有目录、控制器、映射与前端路由核对；原来的采集/治理描述在需求变更后已失效，现已同步为上述行为。此前章节是历史实施记录，其中的采集页面、旧测试结果和已部署状态不代表本次版本。当前使用方式、兼容边界和实际验证记录见 [作者导入说明](author-import.md)。功能实施阶段未重启业务后端；随后按用户试用要求完成本机备份、当前 JAR 启动与真实业务库 V17 升级，见下方本机试用记录。

## 已确认的运行架构

- Windows 根目录提供 `start.bat`、`stop.bat` 一键入口，复用现有 PowerShell 7 启停流程；启动优先使用本机 `.local/Start-LocalProject.ps1`，其他工作区使用标准 Demo 模式。停止保留既有进程归属校验及数据库、容器和运行数据；参数及环境要求见 [开发说明](development.md#构建和入口)。
- 门户 Vue/Vite 独立管理，统一登录地址为 `http://127.0.0.1:18000/login`。两个系统保留独立前后端与业务库，无统一父 POM、npm workspace 或自动跨系统数据同步。
- “爬虫”显示名称改为“信息采集”；`crawler` 内部标识、`systems/crawler/`、`/crawler/`、数据库名和端口保持，避免已有接口与数据迁移。
- 统一认证沿用 crawler 账号、服务端 Session 与权限。网关提供根路径 HttpOnly `PORTAL_SESSION`，relation 在 integration profile 下逐请求确认身份。账号位于 `/management/users`，原 CSRF 与实时权限校验继续生效。
- 门户只允许 relation、crawler 业务地址及实际管理页作为回跳目标；已删除系统的页面、API 和静态资源路径返回 404，不建立代理。
- relation 使用 Java 17 编译目标 / Spring Boot 3.5.16，crawler 使用 Java 21 / Spring Boot 4.1.1；统一运行使用 JDK 21。三个前端工程为门户、relation/frontend、crawler/frontend；crawler 同时构建管理页。
- 新初始化只创建两个 MySQL 8.0.42 与两个 Neo4j 5.26 Community 服务，端口绑定本机。既有被删除系统的容器、数据卷、凭据与历史日志未删除。应用目录在删除前归档至本机忽略的 `.local/subsystem-removal-*/`，保留未提交的用户工作。
- 新环境默认没有应用用户或业务数据；首次启动建立表结构及基础角色/字典。crawler 与 relation 默认关闭管理员引导，开发者按 README 手动在 `course_crawler` 创建统一 admin。已有账号、密码及外部配置保持。
- 可选试用数据导入仅处理 relation 的 30 篇论文及关联样例、crawler 的 12 项成果及治理样例。首次初始化不自动导入，门户装饰统计仍标注演示数据。

## 2026-09-10 知识图谱构建后空白修复

crawler 图谱接口有节点和关系、Neo4j 健康且画布尺寸正常时，仍可能因动效时间单位错误而空白。已核实 CSS 构建把 `280ms` 压缩为 `.28s`，旧 `useMotion` 仅解析数字，vis-network 将其当成毫秒并产生负缩放。共享时长读取现按 `ms` / `s` 换算毫秒，保留默认值、零时长与减少动画偏好，拒绝非法时长。当时接口、图投影、样式令牌和依赖保持原约定；本轮已统一到 Cytoscape，动效单位处理与像素验收要求继续保留。

实施前读取本文件与 `development.md`，并核对当前源码、构建产物、真实图谱接口和画布；未发现这两份记忆与现有系统架构冲突，补充此前缺失的构建时单位差异和像素验收要求。单元测试及图谱浏览器测试覆盖秒单位回归，统一浏览器脚本检查独立入口与门户工作区的真实绘制；不能仅凭接口 200、节点统计或开发模式测试通过判定构建后图谱正常。具体排查与重建命令见 [开发说明](development.md#图谱计数正常但画布空白的排查2026-09-10)。

## 记忆同步与验证边界

本次实施前读取本文件、`development.md`、README、统一登录说明及既有平台管理记录，并核对工作区差异、接入配置、脚本、门户和系统源码。此前四系统说明与修改前代码一致；本次根据删除要求同步为两系统，移除当前开发说明中已删除系统的恢复步骤和运行约定。历史验收统一标明适用阶段，不把历史成功结果当成本次验证结果。

既有平台管理与采集诊断改动在本次开始时尚未提交，已保留。删除系统的未提交文件随目录归档，来源 SHA 不变。未创建新的记忆体系；本次 18 项自动化测试、初始化测试、三个前端构建、来源检查、真实接口与浏览器检查已完成，命令及边界见 [两系统验收](system-scope-acceptance.md)。真实外部信息采集、外部模型调用、Nginx 本体与公网部署不属于本次验收。

## Ye 远端回退

第一阶段开始时核定并导入 `184f651…`；同日 16:18 已观察到远端回退，后续接入阶段的 fetch 再次明确显示强制更新：`184f651… -> bf2494f…`。完整回退目标为 `bf2494f77b96622489ed3b3ca5ee8b206c6c513b`，它是已导入版本的祖先。

接入阶段保留已核定的完整本地源码并完成运行适配，没有以骨架覆盖现有功能。不能将 relation 表述为当前远端 Ye 最新树。后续同步需先核对来源历史，远端回退本身没有被本地修改或撤销。

## 2026-09-10 平台管理与采集诊断

以下记录描述本次删除之前完成的管理与采集诊断工作；当前适用系统范围已同步为 relation、crawler。

用户管理迁到统一门户 /management/users，统一请求日志位于 /management/logs，原登录及采集后台审计位于 /management/audits。仍复用 crawler 账号库、管理服务和权限，不迁移账号或改变角色。crawler 前端多入口构建管理页，集成模式的子系统管理菜单与路由退出业务导航，旧地址回到平台管理。两系统返回入口改为顶部左侧；门户持有同源工作区及整项目全屏，业务路由通过经过来源和路径验证的消息同步，刷新恢复业务地址。

新增网关请求日志使用本机忽略目录保存和有界保留，支持两系统与门户、用户、时间和结果筛选；后台审计与各 JVM 的进程日志边界见开发说明。采集启动异常现有的“只改状态、不写失败明细/审计”缺陷已补齐，错误信息采用固定安全分类，同一终态不会重复记录，不修改表结构或原有数据。

此前平台管理实施前读取本基线、development.md、README 和 unified-login.md，核对当时的干净工作区、源码、已运行 jar 与当前本机库。旧的 /crawler/users 管理位置已按实际新路由同步到开发说明、登录说明和 README。截图运行编号在当前库不存在，现有采集测试和新增事务提交后启动测试均通过，未发现该编号的原始错误证据；该次失败根因仍需截图使用的访问地址/环境确认。没有创建新的记忆体系、修改凭据、升级依赖或执行 Git 提交。详细验证与变更文件见 platform-management.md。

## 2026-09-10 采集可靠性与数据源名称

采集任务、首页任务卡片、质量和治理筛选使用 OpenAlex、Crossref 名称；接口与数据库继续使用来源关联键，避免破坏既有任务和外键。创建、修改和执行均校验来源能力，Crossref 拒绝 OpenAlex 作者/机构标识，多值过滤重复过滤名。

每日计划增加读取、启停和移除，页面自动读取版本；任务提供分页运行历史。旧计划继续使用 `FIXED_SCOPE_REFRESH`。显式增量模式通过 V16 的 `crawl_run_window` 保存实际查询窗口，每次处理最多 7 天：完整成功且没有记录失败才推进，超限时下次缩小，失败或取消重做原窗口。Crossref 按索引时间、OpenAlex 按出版日期处理，不保证补齐迟到记录或全库完整。

重启恢复包含尚未启动的 `PENDING` 与已恢复但尚未附加新 Batch 执行的运行。运行级错误记录安全的阶段、类别和建议，执行失败可从最后已提交检查点重试，并与新触发共用来源锁以避免同范围重复启动。内容及解析器版本不变时仅刷新观测信息，不重建关系或重复发出图投影事件；解析器变化仍触发重建。

实施前核对本基线、development.md 与源码；README 中固定复查和 V15 的描述随本次功能更新为实际行为。完整接口、窗口边界、验证命令和本轮文件清单见 [采集可靠性说明](crawl-reliability.md)。不修改现有业务数据库或凭据；启动新版后端时由 Flyway 执行新增迁移。本轮未提交、推送或重启正在运行的系统。

## 2026-09-10 作者、机构名称选择与关键词说明

OpenAlex 采集表单通过来源名称候选选择作者、机构（各最多50项），提示所属机构或所在地以区分同名对象。已有任务批量回显名称；任务参数仍保存 authorIds/institutionIds，完整URL形式的旧值保留，无新增数据库迁移。Crossref 继续使用 DOI、ORCID、ROR 筛选。

新增只读接口 `GET /api/v1/sources/{sourceId}/entities/{kind}?query=...` 与 `GET /api/v1/sources/{sourceId}/entities/{kind}/resolve?ids=...`，kind 仅允许 authors、institutions，复用 SOURCE_READ 权限并要求启用的 OpenAlex 来源。名称搜索用官方 autocomplete，回显用 ids.openalex 批量过滤；复用认证、请求门控和配额检查，交互等待最多2秒、连接最多3秒、响应最多8秒、响应体最多256KB（不超过原来源限制）。搜索最多10项候选，不自动重试；失败返回503与 SOURCE_LOOKUP_UNAVAILABLE，可手动重试。

前端防抖300毫秒，切换搜索或退出时取消请求并丢弃过期响应。未选中的名称搜索文字、未完成或缺失的名称回显会阻止保存，保留原筛选；用户可明确移除后重选。关键词可选，OpenAlex 传 search 检索标题、摘要及可检索全文，Crossref 传 query 检索成果元数据；整段文本直接交给来源。同类作者/机构多选为或关系，不同筛选维度共同限定作品范围；不保证作者与机构属于同一条署名。

实施前读取本基线、development.md、README 和 crawl-reliability.md，核对源码、既有未提交改动和测试。原记忆与实施前代码一致；本次追加上述交互和接口约定，同步 README 与来源适配哈希。真实外部名称查询与运行中的旧后端替换未执行，测试使用官方格式的模拟响应；验证命令和本轮文件清单见 [采集可靠性说明](crawl-reliability.md#作者机构名称选择补充)。

## 2026-09-10 采集子系统体验与复杂度收敛

本轮默认首页改为工作台，原大屏位于独立 `/dashboard`，集成地址为 `/crawler/dashboard`。目录筛选及分页写入 URL，详情保留返回条件；作者、机构、期刊和主题支持选中 ID 精确匹配或自由文本模糊匹配。导出沿用已显示结果的筛选，后端复用 `CatalogMapper.achievementFilters`，保留旧导出 ID、年份区间与 sourceType 字段。

采集任务列表按创建顺序倒序展示，通过 `/api/v1/crawl/tasks/latest-runs?taskIds=...` 与 `/api/v1/crawl/tasks/schedules?taskIds=...` 批量读取本页状态，每次最多 100 个正整数 ID，分别沿用 CRAWL_RUN_READ 与 CRAWL_SCHEDULE_MANAGE 权限。运行编号统一显示数字 ID，UUID 仅在技术标识中保留。运行详情用处理计数与结束原因表达状态，不再将采集上限当成总量。任务、计划、运行弹窗分别位于 `components/business/crawl/`；高级采集参数默认折叠，数据源校验统一使用 Element Plus。

图谱概览、高级查询、路径分析统一使用 `components/business/GraphCanvas.vue`（Cytoscape）；`utils/graph-presentation.ts` 保留响应验证与类型默认值。关键词和类型筛选不再重复请求，双击进入中心与显式刷新仍受取消、序列校验保护。样式入口改为 `/graph/settings/nodes`、`/graph/settings/edges`，当时旧地址保留为兼容别名；2026-09-11 三类图谱实施后 `/graph/relations` 改为学术关系图谱，关系样式使用正式地址。删除闲置 OperationsView、health 服务、preferences store、motion 工具和旧 vis 画布，移除 vis-network、vis-data、vee-validate、@vee-validate/zod；后端运维、采集检查点、调度及权限功能保持。

实施前读取本文件与另一份指定项目记忆，核对源码及工作区；根目录 `11` 的既有删除未改动。本轮对首页、画布和筛选语义的变更已同步 README，旧图谱空白修复段落属于历史验收，其动效单位约束继续适用。未改数据库结构、凭据或部署配置，未提交、推送或部署。验证证据与剩余边界见 [采集子系统改进记录](crawler-ux-improvements.md)。

运行控制现在会作废先前轮询，避免迟到状态覆盖暂停或取消结果。本轮在用子系统的来源与适配哈希一致；整仓来源命令仍受本机历史 scholar 原始 Git 对象缺失影响。未修改归档 SHA 或跳过历史验证，具体命令及数据库验收限制见上述改进记录。

## 2026-09-11 本机运行方式补充

用户选择在 `E:\Program\Java\course_design\XSGXZSTP-KS` 使用 Windows MySQL80 的两个独立项目库 `course_relation`、`course_crawler`（均为 `127.0.0.1:3306`）；Neo4j 5.26 Community 使用两个独立容器和持久化卷，原集成端口及统一入口 `18000` 保持。默认初始化脚本的 Docker MySQL 方案未改；本实例使用 Git 忽略的 `.local/Start-LocalProject.ps1` 和 `.local/Import-LocalDemoData.mjs`，不会随克隆迁移。凭据通过进程环境提供，未写入源码或项目记忆。运行契约与本机脚本边界见 [开发说明](development.md#2026-09-11-本机-mysql-运行实例)。

本次核对两份指定记忆、现有配置和工作区，保留所有既有源码与锁文件改动。发现原浏览器验收的单画布定位与当前 Cytoscape 多层实现不一致；本机验收入口针对实际节点层验证，未修改原测试断言或业务代码。真实 MySQL 数据、Outbox 图同步、统一登录及图谱绘制已核验；本机验收命令与失败重跑说明保存在 `.local/README-local-runtime.md`。该记录不代表真实外部采集、模型服务或公网部署验收。

## 2026-09-11 成果类型分布中文展示

大屏与统计分析页的成果类型图例、提示和分布表格共用 `systems/crawler/frontend/src/utils/filter-options.ts` 的中文名称，按类型编码转换为期刊论文、综述、预印本、会议论文、图书章节、专著、数据集、报告；未知类型保留接口原标签。筛选选项继续显示编码及中文释义，接口字段、数据库取值和统计数量不变。

## 2026-09-11 本机 Docker 存储状态

本机 Docker Desktop 的 WSL 系统盘和数据盘已迁至 `E:\docker\data`。迁移后暴露的快照元数据库坏页、ext4 结构和镜像内容损坏现已在整盘副本中修复并切换，Docker Engine 29.4.2 与两个项目 Neo4j 容器已恢复。修复前后所有数据卷文件聚合 SHA256 一致，原容器 ID、数据卷及端口保留；认证只读查询已确认原图库可读。迁移前原始备份和本次切换前运行盘均保留在 E 盘，详见 [开发说明](development.md#2026-09-11-本机-docker-数据盘迁移) 与 [修复及后端验收记录](backend-acceptance-20260911.md)。本次没有替换业务后端、迁移 Windows MySQL 项目库或改变应用配置。

## 2026-09-11 当前作者导入边界

当前作者导入已增加多文件自动识别，按“同批同一学者，硕博按导师姓名筛选”约定处理；姓名由本人署名记录的完整作者交集确定，多候选需点选。表内机构自动保存为成果机构，未确认的学者所属关系不补写。多文件预览、整批事务确认与保守身份复用见 `author-import.md`；原单文件 API 和数据库结构不变。包含下述真实表兼容的版本已在本机重新构建运行，最新数据状态见末尾替换记录。

后续真实知网表兼容已通过隔离测试和实际业务库验证：表内多套表头按段解析，中文逗号作者名单拆为独立作者，字段边界 BOM 清理时保留原始列；辑刊按论文保存，科技成果使用 `scientific-result`。主学者优先从论文和专利交集识别，独立单人署名科技成果按原作者归属并复用本批合作作者身份，不补主学者署名。作者两跳子图按共同作品组织取数，仍保持 300 节点上限。完整规则见 [作者导入说明](author-import.md)。

crawler 当前入口为 `/crawler/author-import`，解析用户确认的知网表头与 XLSX/XLS/CSV；论文署名和硕博指导分别建关系，保留原始字段和摘要。数据源、采集任务、数据治理、质量指标四个旧模块的页面及业务 API 已退场，基础表和历史数据保留；此前采集相关段落仅描述对应历史版本。当前接口、权限、V17 迁移与验收结果以 [作者导入说明](author-import.md) 为准。后端验收使用独立测试库，不能将隔离测试成功等同于真实业务实例已完成升级。

## 2026-09-11 本机试用记录

后续按用户启动要求，已在备份本机 `course_crawler` 后运行当前版本，并通过真实数据库查询和 Flyway 日志确认 V16 → V17。门户与两个后端的进程归属、HTTP 健康检查通过，两个原 Neo4j 容器为 `healthy`；访问 `http://127.0.0.1:18000/crawler/author-import` 使用原统一账号登录。备份与运行记录位于 Git 忽略的 `.local/review-start-20260911-171852/`，Docker 运行时套接字恢复及安全输入流程见 [本机试用启动](development.md#2026-09-11-本机试用启动)。这次是业务实例启动检查，真实知网资料的使用反馈仍由用户提供；没有修改业务代码或重新执行完整后端测试。

## 2026-09-11 真实文件替换记录

用户提供的两份知网 XLS 已替换 crawler 原演示学术数据，实际保留 479 条来源记录并去重为 476 项成果：471 篇期刊/辑刊论文、2 篇博士论文、3 项科技成果。211 名作者及 195 家成果机构已入库；席酉民与 210 名共同署名作者构成合作，全库共 546 对合作作者。博士论文按导师筛选约定建立 2 条指导关系；独立科技成果《中国企业的营销渠道控制行为研究》仅归属庄贵军。完整作者顺序、导师归属和 MySQL/Neo4j 数量均已核对，图同步全部成功。

本次只清理 crawler 的 33 张指定学术业务表及托管图谱，账号、角色、配置、历史采集运行和 relation 子系统保留。SQL 与图谱备份、固定文件维护工具和脱敏验证结果位于限制访问、Git 忽略的 `.local/cnki-replacement-20260911/`，备份 SHA256 已匹配；数据库结构仍为 V17。当前前后端已重新构建，crawler 已重启，readiness 为 `UP`，作者导入入口返回 200。43 项不同后端测试、10 项前端测试及构建通过；整仓来源检查仍受既存 retired scholar 对象缺失影响，真实浏览器视觉验收未完成。确切命令、修改范围及恢复边界见 [真实文件替换验收](author-import.md#真实文件替换验收2026-09-11)。
