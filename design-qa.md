# WebUI 设计验收记录

## 验收范围

- 参考图：`C:\Users\likecandy\.codex\attachments\0b486a62-6fc8-4a5d-8186-0de62cb81997\image-1.jpg`
- 实现入口：`http://127.0.0.1:4173/`
- 验收浏览器：Codex 内置浏览器
- 验收视口：`1200 × 900`；并排对照时将两侧统一规范化为 `1600 × 900` 桌面视口后等比缩放
- 验收状态：使用仅用于本地验收的 API 代理提供与现有接口契约一致的管理员数据；生产代码仍只读取真实后端接口，不包含演示数据

## 对照证据

- 全局对照：在同一浏览器页面中并排展示参考图仪表盘区域与实现仪表盘，核对侧栏、顶栏、指标卡、图表矩阵、状态色和信息密度。
- 局部对照：在同一页面中并排放大顶栏、全局筛选与四张指标卡，核对字号层级、间距、边框、圆角和色彩。
- 交互状态：分别检查统计筛选、排行榜排序、采集运行详情、日志流、知识图谱加载和机构节点聚焦。

## 问题与修复

1. 首轮发现采集任务表格在 `1200 × 900` 视口产生横向溢出，优先级为 `P2`。
   - 修复：约束应用外壳、内容区和表格容器的最大宽度，并在页面根节点禁用非预期横向溢出。
   - 复验：采集任务页面不再出现横向滚动，运行追踪输入与操作按钮完整可见。
2. 参考图包含显示器边框与办公室背景，属于展示场景，不属于 WebUI 产品界面。
   - 处理：实现仅匹配显示器内部的产品界面，不引入与产品无关的装饰背景。
3. 参考图右上角使用真人头像，但当前用户接口没有头像字段，优先级为 `P3`。
   - 处理：沿用现有用户身份信息并使用项目已安装图标库中的标准用户图标，避免伪造头像资产。

## 动效验收

- 数据源加载：骨架卡片具有线性微光动画。
- 运行状态：抓取中使用绿色脉冲，异常或重试使用琥珀色呼吸反馈。
- 数值刷新：指标在 `600ms` 内按 `ease-out` 自增；系统设置减弱动效时直接显示最终值。
- 日志流：新增行淡入并从 `translateY(4px)` 归位，容器平滑滚动到底部；轮询结束或组件卸载时释放定时器。
- 图表：折线图、柱状图使用 `600ms` 入场；折线图开启吸附指示线与数据点缩放；筛选期间图表降至 `40%` 透明度并添加 `1px` 模糊。
- 排行榜：排序变化使用 Vue `TransitionGroup` 的 FLIP 位移动画。
- 知识图谱：Cytoscape 使用 Canvas 渲染，力导向布局在 `800ms` 内收敛；悬停时无关元素降至 `0.15`，关联边加粗并使用虚线流动；机构节点点击执行平滑聚焦，新增节点按 `30ms` 间隔渐次显示。
- 无障碍：`prefers-reduced-motion: reduce` 下关闭非必要动画、平滑滚动和图表过渡。

## 最终结论

最终状态：`PASSED`

- `P0`：0
- `P1`：0
- `P2`：0
- `P3`：1 个已接受差异（后端无头像字段，使用标准图标）
- 浏览器错误日志：0

实现已经达到参考图要求的桌面学术数据驾驶舱视觉与交互目标，没有发现阻断交付的视觉、交互或可访问性问题。
