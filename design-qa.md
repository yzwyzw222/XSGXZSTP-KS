# 界面设计与验收要点

本文描述当前 crawler 前端的视觉规则和验收范围，不记录旧门户或多阶段实施过程。历史视觉检查可从 Git 历史查阅，当前页面与运行方式见[项目基线](docs/integration-baseline.md)和[开发说明](docs/development.md)。

## 当前视觉规则

- 全系统固定浅色：浅蓝背景、白色内容区域、深色正文和蓝色主操作。登录页使用校园山水背景，业务页复用统一样式变量。
- 登录页宽屏采用品牌说明与表单双栏，窄屏显示单列表单；密码显隐、表单提示及键盘操作保持可用。
- 工作台和大屏使用同一主导航，窄屏通过抽屉访问模块。成果目录、编目、统计及日志采用紧凑搜索和表头筛选。
- 三类学术图谱使用共享画布和作者条件，给图谱保留主要显示空间；节点、边、证据和时间线以真实接口数据为准，不补造头像、履历、机构或关系。

## 验收范围

1. 在桌面及窄屏核对登录表单、导航、表格、图谱和弹层；不能出现横向溢出、内容遮挡或无法操作的控件。
2. 核对首次加载和历史深色偏好仍显示浅色，并检查输入、焦点、禁用、加载、空数据及错误状态。
3. 使用键盘验证登录、密码显隐、导航、抽屉和关键操作；核对表单提示与可访问名称。
4. 图谱需要检查实际绘制、选择、缩放、适应画布、分页、合作证据及逐篇机构；不能仅凭节点数量或登录跳转判定通过。
5. 核对 Demo 网关返回正式前端产物，Development 使用当前源码；模拟接口与真实账号/数据库的验证结果分别记录。

相关 Playwright 用例位于 `systems/crawler/frontend/e2e`，运行方式见[验证指南](docs/crawler-acceptance.md)。本次文档整理没有重新执行视觉验收，也没有改变前端页面。
